# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $script_path ~/.config/i3/meta_workspaces.py

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork -B=1000 -k --keylayout 2 --indicator --insidecolor ffffffff --insidevercolor ffffffff --datestr="%Y-%m-%d"

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

exec dunst &
exec_always setxkbmap -layout 'no,ru,us' -option 'grp:alt_shift_toggle'
exec_always export QT_QPA_PLATFORMTHEME=qt5ct
exec feh --bg-scale IMG_20190601_125059.jpg &
exec compton --respect-prop-shadow &
exec redshift &
exec albert &
exec polybar example &
exec polybar monitor &
exec libinput-gestures &
exec optimus-manager-qt &

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec termite

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym Mod1+F4 exec .config/rofi/scripts/powermenu.sh
bindsym Mod1+Tab exec "rofi -show window -modi run,drun,window"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+i focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# meta_workspaces by mrconter1
# https://github.com/mrconter1/i3_meta_workspaces

# change to workspace
bindsym $mod+1 exec python $script_path -w 1
bindsym $mod+2 exec python $script_path -w 2
bindsym $mod+3 exec python $script_path -w 3
bindsym $mod+4 exec python $script_path -w 4
bindsym $mod+5 exec python $script_path -w 5
bindsym $mod+6 exec python $script_path -w 6
bindsym $mod+7 exec python $script_path -w 7
bindsym $mod+8 exec python $script_path -w 8
bindsym $mod+9 exec python $script_path -w 9
bindsym $mod+0 exec python $script_path -w 0

# move focused container to workspace
bindsym $mod+Shift+1 exec python $script_path -mw 1
bindsym $mod+Shift+2 exec python $script_path -mw 2
bindsym $mod+Shift+3 exec python $script_path -mw 3
bindsym $mod+Shift+4 exec python $script_path -mw 4
bindsym $mod+Shift+5 exec python $script_path -mw 5
bindsym $mod+Shift+6 exec python $script_path -mw 6
bindsym $mod+Shift+7 exec python $script_path -mw 7
bindsym $mod+Shift+8 exec python $script_path -mw 8
bindsym $mod+Shift+9 exec python $script_path -mw 9
bindsym $mod+Shift+0 exec python $script_path -mw 0

# change meta workspace
bindsym $mod+Control+1 exec python $script_path -m 1
bindsym $mod+Control+2 exec python $script_path -m 2
bindsym $mod+Control+3 exec python $script_path -m 3
bindsym $mod+Control+4 exec python $script_path -m 4
bindsym $mod+Control+5 exec python $script_path -m 5
bindsym $mod+Control+6 exec python $script_path -m 6
bindsym $mod+Control+7 exec python $script_path -m 7
bindsym $mod+Control+8 exec python $script_path -m 8
bindsym $mod+Control+9 exec python $script_path -m 9
bindsym $mod+Control+0 exec python $script_path -m 0

#adjust brightness
bindsym $mod+Mod1+1 exec brightnessctl set 10%
bindsym $mod+Mod1+2 exec brightnessctl set 20%
bindsym $mod+Mod1+3 exec brightnessctl set 30%
bindsym $mod+Mod1+4 exec brightnessctl set 40%
bindsym $mod+Mod1+5 exec brightnessctl set 50%
bindsym $mod+Mod1+6 exec brightnessctl set 60%
bindsym $mod+Mod1+7 exec brightnessctl set 70%
bindsym $mod+Mod1+8 exec brightnessctl set 80%
bindsym $mod+Mod1+9 exec brightnessctl set 90%
bindsym $mod+Mod1+0 exec brightnessctl set 100%

#adjust brightness v2
bindsym $mod+z exec brightnessctl set 5%-
bindsym $mod+Shift+z exec brightnessctl set +5%

#lock
bindsym $mod+x exec i3lock -B=1000 -k --keylayout 2 --indicator --insidecolor ffffffff --insidevercolor ffffffff --datestr="%Y-%m-%d"

# next/previous workspace
bindsym $mod+b workspace back_and_forth
bindsym $mod+Shift+b move container to workspace back_and_forth

#enable switching from one workspace to another using the same command
#eg.: W1 -> $mod+2 -> W2 -> $mod+2 -> W1
workspace_auto_back_and_forth yes

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 1 px or 1 ppt
        bindsym k resize grow height 1 px or 1 ppt
        bindsym i resize shrink height 1 px or 1 ppt
        bindsym l resize grow width 1 px or 1 ppt

 	bindsym Shift+j resize shrink width 10 px or 10 ppt
        bindsym Shift+k resize grow height 10 px or 10 ppt
        bindsym Shift+i resize shrink height 10 px or 10 ppt
        bindsym Shift+l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize grow height 1 px or 1 ppt
        bindsym Up resize shrink height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

	bindsym Shift+Left resize shrink width 10 px or 10 ppt
        bindsym Shift+Down resize grow height 10 px or 10 ppt
        bindsym Shift+Up resize shrink height 10 px or 10 ppt
        bindsym Shift+Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# resize gaps (I'm so proud of myself for this)
mode "change gaps" {
	bindsym Down gaps inner current minus 3
	bindsym Up gaps inner current plus 3
	bindsym Shift+Down gaps outer current minus 3
	bindsym Shift+Up gaps outer current plus 3
	bindsym $mod+g mode "default"
	bindsym Return mode "default"
	bindsym $mod+r mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+g mode "change gaps"

#app launching mode
mode "launch apps" {
	bindsym f exec firefox --new-instance  
	bindsym t exec streamlink-twitch-gui  
	bindsym a exec atom  
	bindsym b exec blueberry  
	bindsym g exec gimp  
	bindsym p exec pcmanfm  
	bindsym m exec thunderbird  
	bindsym x exec xpaint  
	bindsym s exec steam  
	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+n mode "default"
} 
bindsym $mod+n mode "launch apps" 

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

new_window 1pixel

#here be gaps
gaps inner 9
smart_borders no_gaps
smart_gaps on

#here be rounded
border_radius 9

#borders begone
default_border pixel 0
hide_edge_borders both

#scratchpad
bindsym $mod+Shift+t move scratchpad
bindsym $mod+t scratchpad show

# minim-m and minim-r by NatKarmios
# https://gist.github.com/NatKarmios/e711d33894b27c44abbcc2a92a85f17d
# (someone help me I don't know how this works)
bindsym $mod+m exec ~/.config/i3/minim-m.sh
bindsym $mod+Shift+m exec ~/.config/i3/minim-r.sh
